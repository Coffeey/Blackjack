# Blackjack

A console blackjack game in Python. Betting, 3:2 naturals,
and stats that persist between sessions.

## Running it

    python main.py

Requires Python 3.12 or newer.

## Controls

- `h` to hit, `s` to stay
- Bets must be even numbers
